# PLAN-IBIS-165: Add JSDoc documentation to IFCConverterScene component

**Ticket**: [IBIS-165](https://betekk.atlassian.net/browse/IBIS-165)
**Branch**: `IBIS-165`
**File**: `src/components/IFCConverter/IFCConverterScene.tsx`
**Type**: Documentation only (no behavioral changes)

---

## Overview

Add comprehensive JSDoc/TSDoc docstrings to all exported types, interfaces, functions, and the main component in `IFCConverterScene.tsx`. The file is a 276-line React client component that provides an IFC-to-Fragments 3D conversion scene using Three.js and `@thatopen/components`.

---

## Implementation Phases

### Phase 1: Review & Setup
- [ ] Review current `IFCConverterScene.tsx` for all undocumented items
- [ ] Load `docstring-generator` skill for JSDoc/TSDoc standards
- [ ] Verify no behavioral changes are introduced

### Phase 2: Type & Interface Docstrings
- [ ] Add JSDoc to `ModelStatus` type (line 11) — document union values
- [ ] Add TSDoc to `ModelEntry` interface (lines 13-18) — document each property

### Phase 3: Function Docstrings
- [ ] Add JSDoc to `handleFiles` async function (lines 32-79) — `@param files`, `@returns`, `@throws`
- [ ] Add JSDoc to `downloadFrag` async function (lines 81-99) — `@param modelId`, side effects
- [ ] Add JSDoc to `removeModel` function (lines 101-104) — `@param modelId`, cleanup behavior
- [ ] Add JSDoc to `init` async function (lines 112-200) — scene setup, Three.js dependencies, event listeners

### Phase 4: Component Docstring
- [ ] Add JSDoc to `IFCConverterScene` component (line 20) — purpose, behavior, dependencies (`@thatopen/components`, Three.js), side effects (DOM manipulation, scene initialization, event listeners), return value

### Phase 5: Validation & Cleanup
- [ ] Verify TypeScript compilation still passes (`tsc --noEmit`)
- [ ] Verify no lint errors introduced
- [ ] Confirm all docstrings follow JSDoc/TSDoc conventions
- [ ] Verify acceptance criteria are met

---

## Acceptance Criteria

- [ ] `ModelStatus` type has a JSDoc comment explaining the union values
- [ ] `ModelEntry` interface has a TSDoc comment documenting each property
- [ ] `IFCConverterScene` component has a JSDoc block describing its purpose, behavior, dependencies, and side effects
- [ ] `handleFiles` has a JSDoc block with `@param` and `@returns` / `@throws` tags
- [ ] `downloadFrag` has a JSDoc block with `@param` and side-effect documentation
- [ ] `removeModel` has a JSDoc block with `@param` and cleanup behavior documented
- [ ] `init` has a JSDoc block describing scene initialization, Three.js setup, and event listener registration
- [ ] All docstrings follow JSDoc/TSDoc conventions for TypeScript React components
- [ ] No behavioral changes to the component — documentation only

---

## Technical Notes

- Use the `docstring-generator` skill during implementation
- Docstrings should cover: component purpose, parameter descriptions, return types, side effects (DOM manipulation, Three.js scene setup, event listeners), and dependencies (`@thatopen/components`, Three.js APIs)
- This is a **documentation-only** change

---

## Dependencies

None — this is a standalone documentation task.

---

## Risks & Mitigation

| Risk | Mitigation |
|------|------------|
| Docstrings may reference internal APIs that change | Keep descriptions at a high level; avoid coupling to implementation details |
| Inconsistent documentation style | Use `docstring-generator` skill for standards compliance |

---

*Generated by ticket-plan-workflow-skill*
